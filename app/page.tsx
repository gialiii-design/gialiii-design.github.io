import Link from 'next/link';
import {projects} from './projects';
import {Header,Footer} from './shared';
export default function Home(){return <><Header/><main id="main"><section className="hero wrap"><div className="eyebrow">● PRODUCT / UX / UI DESIGN</div><div className="hero-name"><h1>Gia Li<span>.</span></h1><span>李佳玲</span></div><div className="hero-bottom"><p>从理解问题，<br/>到让体验清晰可用。</p><div><p className="muted">关注用户需求与业务之间的连接，<br/>探索产品体验、交互设计与视觉表达。</p><a className="text-link" href="#work">浏览精选作品 ↓</a></div></div></section><section id="work" className="wrap"><div className="section-label"><h2>精选作品 <span>Selected work</span></h2><span>01 — 03 / + EXPLORATION</span></div><div className="project-grid">{projects.map(p=><Link className={`project-card ${p.slug}`} key={p.slug} href={`/work/${p.slug}`}><div className="project-image"><img src={`/projects/${p.slug}-0.webp`} alt={p.title+'项目展示'} width="1920" height="1080" loading="lazy"/><span aria-hidden="true">↗</span></div><div className="project-meta"><span>{p.number}</span><span>{p.category}</span></div><h3>{p.title}</h3><p>{p.subtitle}</p></Link>)}</div></section><section id="about" className="about-section wrap"><div className="eyebrow">ABOUT / 关于我</div><div><h2>在逻辑与表达之间，<br/>寻找合适的设计。</h2><p>我是李佳玲 Gia Li，设计方向为产品 / UX / UI。我的作品涵盖留学准备服务、电商体验评估、团险业务流程，以及 AI 辅助营销视觉探索。</p><p>我关注设计如何帮助人理解信息、做出选择，并顺利完成任务。</p><div className="skills">{['用户体验研究','交互与流程设计','界面设计','视觉表达'].map(s=><span key={s}>{s}</span>)}</div></div></section></main><Footer/></>}
